package com.dn.jpa.oto.repository;

import com.dn.jpa.oto.model.DnBookDetail;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IDnBookDetailRepository extends JpaRepository<DnBookDetail,Long>{
}